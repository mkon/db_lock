sudo: required

language: ruby

services:
  - mysql
  - docker

rvm:
  - '2.4'
  - '2.5'
  - '2.6'

branches:
  only:
    - master

env:
  - DB=mysql
  - DB=sqlserver

install:
  - ci/install

before_script:
  - ci/before

script:
  - ci/run
